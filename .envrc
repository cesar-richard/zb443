# Configuration environnement ESP-IDF pour direnv
# Installez direnv: brew install direnv (macOS) ou apt install direnv (Ubuntu)
# Puis: direnv allow

# Chemin vers ESP-IDF (ajustez selon votre installation)
export IDF_PATH="$HOME/esp-idf"

# Charger l'environnement ESP-IDF
if [ -f "$IDF_PATH/export.sh" ]; then
    source "$IDF_PATH/export.sh"
    echo "✅ ESP-IDF v$(idf.py --version | cut -d' ' -f3) chargé"
else
    echo "❌ ESP-IDF non trouvé dans $IDF_PATH"
    echo "Installez ESP-IDF v5.2.x et ajustez IDF_PATH dans ce fichier"
fi

# Configuration du projet
export PROJECT_NAME="zb433"
export PROJECT_PATH="$(pwd)"

echo "🚀 Projet $PROJECT_NAME configuré dans $PROJECT_PATH"
